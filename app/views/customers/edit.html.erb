<%= form_for @customer, url: customer_path(@customer), html: {class: "edit-form"},:action => "update" do |f| %>
  <%= f.label :Email %>
  <%= f.text_field :email %>

  <%= f.submit 'Update customer', class: 'btn btn-primary' %>
<% end %>
